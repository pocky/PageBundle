{% block script %}
<script type="text/javascript">
    {% block js %}
    jQuery(document).ready(function() {
        $('#slug').slugify('{{ "#" ~ form.name.vars.id }}')
        $('{{ "#" ~ form.slug.vars.id }}').slugify('{{ "#" ~ form.name.vars.id }}')
    });
    {% endblock %}
</script>
{% endblock %}
